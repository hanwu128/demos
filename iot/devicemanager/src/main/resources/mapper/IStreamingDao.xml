<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lenovo.iot.devicemanager.dao.IStreamingDao">
	<insert id="doInsertStreaming" useGeneratedKeys="true" keyProperty="id" parameterType="StreamingVO">
	INSERT INTO
		streaming(
					 `streamingname`,
					  streamingdesc,
					  createtimeat
					  )
	VALUES
		(#{streamingname},
		 #{streamingdesc},
		 #{createtimeat}
		)
	</insert>

	<select id="findAllStreaming" resultType="com.lenovo.iot.devicemanager.model.bo.StreamingVO">
		SELECT 
			id,
			`streamingname`,
			streamingdesc,
			createtimeat
		FROM 
			streaming t
	</select>

	<select id="findStreamingByParameter" resultType="com.lenovo.iot.devicemanager.model.bo.StreamingVO" parameterType="StreamingVO">
		SELECT
		id,
		`streamingname`,
		streamingdesc,
		createtimeat
		FROM
		streaming

		WHERE
		1 = 1
		<if test="id != null and id != '' and id != 0">
			AND  id = #{id}
		</if>

	</select>

</mapper>