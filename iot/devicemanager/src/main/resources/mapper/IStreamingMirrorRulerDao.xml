<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lenovo.iot.devicemanager.dao.IStreamingMirrorRulerDao">
	<insert id="doInsertStreamingMirrorRuler" useGeneratedKeys="true" keyProperty="id" parameterType="StreamingMirrorRulerVO">
	INSERT INTO
		streamingmirrorruler(
					 `streamingdevappbriprimarykey`,
					  streamingid,
					  deviceid,
					  rulergroupid
					  )
	VALUES
		(#{streamingdevappbriprimarykey},
		 #{streamingid},
		 #{deviceid},
		 #{rulergroupid}
		)
	</insert>

	<select id="findStreamingMirrorRulerVOByParameter" resultType="com.lenovo.iot.devicemanager.model.bo.StreamingMirrorRulerVO" parameterType="StreamingMirrorRulerVO">
		SELECT
		id,
		`streamingdevappbriprimarykey`,
		streamingid,
		deviceid,
		rulergroupid

		FROM
		streamingmirrorruler t

		WHERE
		1 = 1
		<if test="streamingdevappbriprimarykey != 0">
			AND  streamingdevappbriprimarykey = #{streamingdevappbriprimarykey}
		</if>



	</select>

</mapper>