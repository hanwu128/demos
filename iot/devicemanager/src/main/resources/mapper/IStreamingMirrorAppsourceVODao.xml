<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lenovo.iot.devicemanager.dao.IStreamingMirrorAppsourceVODao">
	<insert id="doInsertStreamingMirrorAppsourceVO" useGeneratedKeys="true" keyProperty="id" parameterType="StreamingMirrorAppsourceVO">
	INSERT INTO
		streamingmirrorappsource(
					 `streamingdevappbriprimarykey`,
					  sourcename,
					  sourcedisplayname,
					  mapname,
					  mapdisplayname,
					  sourcedefault,
					  sourcedatatype,
					  sourceunit
					  )
	VALUES
		(#{streamingdevappbriprimarykey},
		 #{sourcename},
		 #{sourcedisplayname},
		 #{mapname},
		 #{mapdisplayname},
		 #{sourcedefault},
		 #{sourcedatatype},
		 #{sourceunit}
		)
	</insert>

	<select id="findStreamingMirrorAppsourceVOByParameter" resultType="com.lenovo.iot.devicemanager.model.bo.StreamingMirrorAppsourceVO" parameterType="StreamingMirrorAppsourceVO">
		SELECT
		id,
		streamingdevappbriprimarykey,
		sourcename,
		sourcedisplayname,
		mapname,
		mapdisplayname,
		ifnull(sourcedefault,'') as sourcedefault,
		ifnull(sourcedatatype,'') as sourcedatatype,
		ifnull(sourceunit,'') as sourceunit
		FROM streamingmirrorappsource
		WHERE
		1 = 1
		<if test="id != null and id != 0">
			AND  id = #{id}
		</if>
		<if test="streamingdevappbriprimarykey != null and streamingdevappbriprimarykey != 0">
			AND  streamingdevappbriprimarykey = #{streamingdevappbriprimarykey}
		</if>

	</select>

</mapper>